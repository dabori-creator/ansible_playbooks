# Ansible playbooks

Ansible playbooks for my workflow:

1. password-policy - generate a new password if the current password expires

2. grafana-exporters - install exporters (node exporter, cadvisor exporter, postgres exporter, blackbox exporter, x509 exporter)

3. rotate-keys - automate the rotation of SSH Ed25519 keys
